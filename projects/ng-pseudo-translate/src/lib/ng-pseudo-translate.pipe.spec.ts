import { NgPseudoTranslatePipe } from './ng-pseudo-translate.pipe';

describe('NgPseudoTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new NgPseudoTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
